library(rbbt)

path <- http://127.0.0.1:23119/better-bibtex/export/collection?/1/QZN3EP44.json

cit <- bbt_detect_citations(path, )
cit
bbt_libraries()


library(quanteda)
library(readtext)

help(quanteda)
  
(rtjsna <- readtext(paste0(/pdfdissertation/september/*/*.pdf"), 
                 docvarsfrom = "filenames", 
                 docvarnames = c("document", "language")))
Encoding(rt7$text)
